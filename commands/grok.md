---
description: Delegate to a Grok 4.6 peer (shared thread, RESULT + transcript)
---

Run `peer grok` (or `grok46`). It prints RESULT, TRANSCRIPT, RUN, THREAD.
Read RESULT first. Read TRANSCRIPT if the card is thin or blocked.
Same repo = same thread. `--note` for what you already know.
After a two-brain pass: `peer huddle`.

If no task is given, ask what to send.

~~~bash
peer grok $ARGUMENTS
~~~
