---
description: Send work to a different coding plan. Never call yourself.
---

`peers` is the other plan. Native Task for your own model.
Watch bash heartbeats. Read RESULT. Don't cat unless the memo is thin.

  peers auto "task"
  peers research "question that needs depth"
  peers both --with a,b,c "question"    # names: peers providers
  peers huddle
  peers doctor

If STATUS: auth: peers has no account. `peers doctor`.
If that peer is ok, retry it once (Cursor's "Starting login process" is a banner).
If AUTH: run that CLI's login in a real terminal, then doctor, then retry.
Don't substitute a different prior.

~~~bash
peers $ARGUMENTS
~~~
